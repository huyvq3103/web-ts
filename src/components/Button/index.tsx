import { Row } from "antd";
import { Wrapper } from "./styles";

const Button = () => {
  return (
    <Row style={{ display: "flex", justifyContent: "center" }}>
      <Wrapper>San pham</Wrapper>
    </Row>
  );
};

export default Button;
